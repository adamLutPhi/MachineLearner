@propagate_inbounds function divideConquer(arr = [1, 2, 3, 4], a = 1, b = length(arr))  # a code that never walks..  a code that never runs       
#=
by looking at it first, everything might sound easy , and nothing would seem wrong for the untrained eye (me I guess pshhhhhhh)
 so, no matter how much iteration it runs... it woun't move an inch towards solving that problem :LOL 

=#